/**
 * Command Help Extraction Utility
 *
 * Automatically extracts and formats command-line help documentation from workflow tools.
 */
/**
 * Tool configuration
 */
export interface Tool {
    name: string;
    command: string;
    directory: string;
    helpFlag?: string;
}
/**
 * Help extraction result
 */
export interface HelpNode {
    command: string;
    help: string;
    children: HelpNode[];
}
/**
 * Detect available tools in the repository
 */
export declare function detectTools(repoRoot: string, tools?: Tool[]): Tool[];
/**
 * Execute help command and capture output.
 * Tries the primary help flag first, then falls back to alternative strategies.
 */
export declare function executeHelp(command: string, helpFlag?: string, timeout?: number): Promise<string>;
/**
 * Detect subcommands from help text
 */
export declare function detectSubcommands(helpText: string): string[];
/**
 * Reset the failure tracker (useful between tools or test runs).
 */
export declare function resetExtractionFailures(): void;
/**
 * Return a copy of the recorded failures.
 */
export declare function getExtractionFailures(): string[];
/**
 * Recursively extract help for command and subcommands
 */
export declare function extractHelpRecursive(command: string, subcommand?: string[], depth?: number, maxDepth?: number, helpFlag?: string): Promise<HelpNode>;
/**
 * Extract help for all detected tools
 */
export declare function extractAllHelp(repoRoot: string): Promise<Map<Tool, HelpNode>>;
/**
 * Generate Markdown output from help nodes
 */
export declare function generateMarkdown(helpMap: Map<Tool, HelpNode>): string;
/**
 * Main extraction function
 */
export declare function extractCommandHelp(repoRoot: string, outputPath?: string): Promise<string>;
//# sourceMappingURL=extractCommandHelp.d.ts.map