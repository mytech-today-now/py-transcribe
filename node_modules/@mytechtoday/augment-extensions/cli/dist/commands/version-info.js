"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.versionInfoCommand = versionInfoCommand;
const chalk_1 = __importDefault(require("chalk"));
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const module_loader_1 = require("../core/module-loader");
const version_manager_1 = require("../core/version-manager");
const compatibility_checker_1 = require("../core/compatibility-checker");
const module_system_1 = require("../utils/module-system");
async function versionInfoCommand(moduleName, options = {}) {
    try {
        const { json = false, changelog = true, compatibility = true } = options;
        const modules = (0, module_system_1.discoverModules)();
        const module = modules.find((m) => m.fullName === moduleName || m.metadata.name === moduleName);
        if (!module) {
            if (json) {
                console.log(JSON.stringify({ error: `Module not found: ${moduleName}` }, null, 2));
            }
            else {
                console.error(chalk_1.default.red(`✗ Module not found: ${moduleName}`));
                console.log(chalk_1.default.gray('\nAvailable modules:'));
                modules.forEach((m) => console.log(chalk_1.default.gray(`  - ${m.fullName}`)));
            }
            process.exit(1);
        }
        const versionManager = new version_manager_1.VersionManager();
        const metadata = versionManager.getVersion(module.path);
        if (!metadata) {
            if (json) {
                console.log(JSON.stringify({ error: 'Unable to determine version information' }, null, 2));
            }
            else {
                console.error(chalk_1.default.red('✗ Unable to determine version information'));
            }
            process.exit(1);
        }
        const loader = new module_loader_1.ModuleLoader();
        const availableVersions = loader.getAvailableVersions(module.path);
        const compatResult = compatibility ? new compatibility_checker_1.CompatibilityChecker().checkCompatibility(module.path) : null;
        let changelogContent = null;
        if (changelog) {
            const changelogPath = path.join(module.path, 'CHANGELOG.md');
            if (fs.existsSync(changelogPath)) {
                changelogContent = fs.readFileSync(changelogPath, 'utf-8');
            }
        }
        if (json) {
            console.log(JSON.stringify({
                module: moduleName,
                version: metadata.version,
                metadata: {
                    deprecated: metadata.deprecated,
                    deprecationMessage: metadata.deprecationMessage,
                    breaking: metadata.breaking,
                    publishedAt: metadata.publishedAt,
                    changelog: metadata.changelog
                },
                availableVersions,
                compatibility: compatResult,
                changelog: changelogContent
            }, null, 2));
            return;
        }
        console.log(chalk_1.default.bold.blue(`\n📦 ${moduleName} - Version Information\n`));
        console.log(chalk_1.default.bold('Version:'));
        console.log(`  ${chalk_1.default.green(metadata.version)}`);
        if (metadata.publishedAt) {
            console.log(`  ${chalk_1.default.gray('Released:')} ${new Date(metadata.publishedAt).toLocaleDateString()}`);
        }
        if (metadata.deprecated) {
            console.log(chalk_1.default.yellow('\n⚠ Status: DEPRECATED'));
            if (metadata.deprecationMessage) {
                console.log(chalk_1.default.yellow(`  ${metadata.deprecationMessage}`));
            }
        }
        if (metadata.breaking) {
            console.log(chalk_1.default.red('\n⚠ Contains Breaking Changes'));
        }
        if (availableVersions.length > 0) {
            console.log(chalk_1.default.bold('\nAvailable Versions:'));
            availableVersions.forEach((v) => {
                const isCurrent = v === metadata.version;
                console.log(`  ${isCurrent ? chalk_1.default.green('●') : chalk_1.default.gray('○')} ${v}${isCurrent ? chalk_1.default.gray(' (current)') : ''}`);
            });
        }
        if (compatResult) {
            console.log(chalk_1.default.bold('\nCompatibility:'));
            console.log(compatResult.compatible
                ? chalk_1.default.green('  ✓ Compatible with current environment')
                : chalk_1.default.red('  ✗ Compatibility issues detected'));
            if (compatResult.details.node) {
                const node = compatResult.details.node;
                console.log(`  ${chalk_1.default.gray('Node.js:')} ${node.current} ${node.compatible ? chalk_1.default.green('✓') : chalk_1.default.red('✗')} (requires ${node.required}+)`);
            }
            if (compatResult.details.typescript) {
                const ts = compatResult.details.typescript;
                console.log(`  ${chalk_1.default.gray('TypeScript:')} ${ts.current} ${ts.compatible ? chalk_1.default.green('✓') : chalk_1.default.red('✗')} (requires ${ts.required}+)`);
            }
            if (compatResult.warnings.length > 0) {
                console.log(chalk_1.default.yellow('\n  Warnings:'));
                compatResult.warnings.forEach((w) => console.log(chalk_1.default.yellow(`    - ${w}`)));
            }
            if (compatResult.errors.length > 0) {
                console.log(chalk_1.default.red('\n  Errors:'));
                compatResult.errors.forEach((e) => console.log(chalk_1.default.red(`    - ${e}`)));
            }
        }
        if (changelogContent) {
            console.log(chalk_1.default.bold('\nChangelog:'));
            const lines = changelogContent.split('\n').slice(0, 20);
            lines.forEach((line) => console.log(chalk_1.default.gray(`  ${line}`)));
            const totalLines = changelogContent.split('\n').length;
            if (totalLines > 20) {
                console.log(chalk_1.default.gray(`  ... (${totalLines - 20} more lines)`));
                console.log(chalk_1.default.gray(`  View full changelog: ${path.join(module.path, 'CHANGELOG.md')}`));
            }
        }
        else if (metadata.changelog) {
            console.log(chalk_1.default.bold('\nChangelog:'));
            console.log(chalk_1.default.gray(`  ${metadata.changelog}`));
        }
    }
    catch (error) {
        if (options.json) {
            console.log(JSON.stringify({ error: String(error) }, null, 2));
        }
        else {
            console.error(chalk_1.default.red('Error getting version information:'), error);
        }
        process.exit(1);
    }
}
//# sourceMappingURL=version-info.js.map