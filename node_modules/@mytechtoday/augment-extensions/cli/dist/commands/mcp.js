"use strict";
/**
 * MCP Commands
 *
 * Commands for managing and using MCP server integrations.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.mcpListCommand = mcpListCommand;
exports.mcpAddCommand = mcpAddCommand;
exports.mcpRemoveCommand = mcpRemoveCommand;
exports.mcpExecCommand = mcpExecCommand;
exports.mcpWrapCommand = mcpWrapCommand;
exports.mcpDiscoverCommand = mcpDiscoverCommand;
exports.mcpGenerateCLICommand = mcpGenerateCLICommand;
const chalk_1 = __importDefault(require("chalk"));
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const mcp_integration_1 = require("../utils/mcp-integration");
/**
 * List all configured MCP servers
 */
async function mcpListCommand(options = {}) {
    try {
        const configs = (0, mcp_integration_1.loadMCPConfigs)();
        if (configs.length === 0) {
            console.log(chalk_1.default.yellow('No MCP servers configured'));
            console.log(chalk_1.default.gray('\nAdd a server with: augx mcp add <name> <command>'));
            return;
        }
        if (options.json) {
            console.log(JSON.stringify(configs, null, 2));
            return;
        }
        console.log(chalk_1.default.blue('\n🔌 Configured MCP Servers\n'));
        console.log(chalk_1.default.gray('─'.repeat(50)));
        for (const config of configs) {
            console.log(chalk_1.default.green(`  ${config.name}`));
            console.log(chalk_1.default.gray(`    Command: ${config.command}`));
            console.log(chalk_1.default.gray(`    Transport: ${config.transport}`));
            if (config.url) {
                console.log(chalk_1.default.gray(`    URL: ${config.url}`));
            }
            if (config.args && config.args.length > 0) {
                console.log(chalk_1.default.gray(`    Args: ${config.args.join(' ')}`));
            }
        }
        console.log(chalk_1.default.gray('\n' + '─'.repeat(50)));
        console.log(chalk_1.default.blue(`Total: ${configs.length} servers\n`));
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error listing MCP servers: ${error}`));
        process.exit(1);
    }
}
/**
 * Add MCP server configuration
 */
async function mcpAddCommand(name, command, options = {}) {
    try {
        const config = {
            name,
            command,
            transport: options.transport || 'stdio',
            args: options.args ? options.args.split(' ') : undefined,
            url: options.url,
            env: options.env ? JSON.parse(options.env) : undefined
        };
        (0, mcp_integration_1.addMCPServer)(config);
        console.log(chalk_1.default.green(`✓ Added MCP server: ${name}`));
        console.log(chalk_1.default.gray(`  Command: ${command}`));
        console.log(chalk_1.default.gray(`  Transport: ${config.transport}`));
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error adding MCP server: ${error}`));
        process.exit(1);
    }
}
/**
 * Remove MCP server configuration
 */
async function mcpRemoveCommand(name) {
    try {
        const removed = (0, mcp_integration_1.removeMCPServer)(name);
        if (!removed) {
            console.error(chalk_1.default.red(`MCP server not found: ${name}`));
            process.exit(1);
        }
        console.log(chalk_1.default.green(`✓ Removed MCP server: ${name}`));
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error removing MCP server: ${error}`));
        process.exit(1);
    }
}
/**
 * Execute MCP tool
 */
async function mcpExecCommand(serverName, toolName, options = {}) {
    try {
        const args = options.args ? JSON.parse(options.args) : {};
        console.log(chalk_1.default.blue(`Executing MCP tool: ${serverName}/${toolName}`));
        console.log(chalk_1.default.gray(`Args: ${JSON.stringify(args)}\n`));
        const result = await (0, mcp_integration_1.executeMCPCommand)(serverName, toolName, args);
        if (options.json) {
            console.log(JSON.stringify(result, null, 2));
        }
        else {
            console.log(chalk_1.default.green('\n✓ Result:'));
            console.log(JSON.stringify(result, null, 2));
        }
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error executing MCP tool: ${error}`));
        process.exit(1);
    }
}
/**
 * Generate skill wrapper for MCP tool
 */
async function mcpWrapCommand(serverName, toolName, skillId, options = {}) {
    try {
        const category = options.category || 'integration';
        const skillContent = (0, mcp_integration_1.generateMCPSkillWrapper)(serverName, toolName, skillId, category);
        // Determine output path
        const skillsDir = path.join(process.cwd(), 'skills', category);
        if (!fs.existsSync(skillsDir)) {
            fs.mkdirSync(skillsDir, { recursive: true });
        }
        const outputPath = path.join(skillsDir, `${skillId}.md`);
        fs.writeFileSync(outputPath, skillContent, 'utf-8');
        console.log(chalk_1.default.green(`✓ Generated skill wrapper: ${skillId}`));
        console.log(chalk_1.default.gray(`  Server: ${serverName}`));
        console.log(chalk_1.default.gray(`  Tool: ${toolName}`));
        console.log(chalk_1.default.gray(`  Category: ${category}`));
        console.log(chalk_1.default.gray(`  Output: ${outputPath}`));
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error generating skill wrapper: ${error}`));
        process.exit(1);
    }
}
/**
 * Discover tools from MCP server
 */
async function mcpDiscoverCommand(serverName, options = {}) {
    try {
        console.log(chalk_1.default.blue(`Discovering tools from MCP server: ${serverName}...`));
        const tools = await (0, mcp_integration_1.discoverMCPTools)(serverName);
        if (tools.length === 0) {
            console.log(chalk_1.default.yellow('\nNo tools discovered (or discovery not yet implemented)'));
            return;
        }
        if (options.json) {
            console.log(JSON.stringify(tools, null, 2));
            return;
        }
        console.log(chalk_1.default.blue(`\n🔍 Discovered Tools from ${serverName}\n`));
        console.log(chalk_1.default.gray('─'.repeat(50)));
        for (const tool of tools) {
            console.log(chalk_1.default.green(`  ${tool.name}`));
            console.log(chalk_1.default.gray(`    ${tool.description}`));
        }
        console.log(chalk_1.default.gray('\n' + '─'.repeat(50)));
        console.log(chalk_1.default.blue(`Total: ${tools.length} tools\n`));
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error discovering tools: ${error}`));
        process.exit(1);
    }
}
/**
 * Generate CLI using mcporter
 */
async function mcpGenerateCLICommand(serverCommand, outputPath) {
    try {
        if (!(0, mcp_integration_1.isMCPorterAvailable)()) {
            console.error(chalk_1.default.red('mcporter is not available'));
            console.log(chalk_1.default.yellow('\nInstall mcporter with: npm install -g mcporter'));
            process.exit(1);
        }
        console.log(chalk_1.default.blue(`Generating CLI with mcporter...`));
        console.log(chalk_1.default.gray(`  Server: ${serverCommand}`));
        console.log(chalk_1.default.gray(`  Output: ${outputPath}\n`));
        await (0, mcp_integration_1.generateCLIWithMCPorter)(serverCommand, outputPath);
        console.log(chalk_1.default.green(`\n✓ CLI generated successfully`));
        console.log(chalk_1.default.gray(`  Output: ${outputPath}`));
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error generating CLI: ${error}`));
        process.exit(1);
    }
}
//# sourceMappingURL=mcp.js.map