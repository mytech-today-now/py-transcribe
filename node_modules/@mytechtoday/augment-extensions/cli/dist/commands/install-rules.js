"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.installRulesCommand = installRulesCommand;
const chalk_1 = __importDefault(require("chalk"));
const path = __importStar(require("path"));
const install_rules_1 = require("../utils/install-rules");
const rule_install_hooks_1 = require("../utils/rule-install-hooks");
/**
 * Install character count management rule
 */
async function installRulesCommand(options = {}) {
    const { quiet = false, setupHooks = false, removeHooks = false, gitHookType = 'post-checkout', force = false, interactive = false } = options;
    try {
        if (removeHooks) {
            // Remove hooks
            if (!quiet) {
                console.log(chalk_1.default.blue('\n🔧 Removing rule installation hooks...\n'));
            }
            const gitDir = path.join(process.cwd(), '.git');
            await (0, rule_install_hooks_1.removeRuleInstallGitHook)(gitHookType, gitDir);
            await (0, rule_install_hooks_1.removeNpmPostInstallHook)(path.join(process.cwd(), 'package.json'));
            if (!quiet) {
                console.log(chalk_1.default.green('\n✓ Rule installation hooks removed\n'));
            }
            return;
        }
        if (setupHooks) {
            // Setup hooks
            if (!quiet) {
                console.log(chalk_1.default.blue('\n🔧 Setting up rule installation hooks...\n'));
            }
            const gitDir = path.join(process.cwd(), '.git');
            await (0, rule_install_hooks_1.createRuleInstallGitHook)(gitHookType, gitDir);
            await (0, rule_install_hooks_1.createNpmPostInstallHook)(path.join(process.cwd(), 'package.json'));
            if (!quiet) {
                console.log(chalk_1.default.green('\n✓ Rule installation hooks configured\n'));
                console.log(chalk_1.default.gray('The character count rule will be automatically installed on:'));
                console.log(chalk_1.default.gray(`  - Git ${gitHookType}`));
                console.log(chalk_1.default.gray('  - npm install (postinstall)\n'));
            }
            return;
        }
        // Install rule
        if (!quiet) {
            console.log(chalk_1.default.blue('\n📏 Installing character count management rule...\n'));
        }
        const result = await (0, install_rules_1.installCharacterCountRule)({
            targetDir: process.cwd(),
            skipIfExists: !force,
            verbose: !quiet,
            force,
            interactive
        });
        if (!result.success) {
            if (!quiet) {
                console.error(chalk_1.default.red(`\n✗ Failed to install rule: ${result.error}\n`));
                // Show error-specific guidance
                if (result.errorType === 'PERMISSION_DENIED') {
                    console.error(chalk_1.default.yellow('Tip: Check file permissions or run with appropriate privileges\n'));
                }
                else if (result.errorType === 'DISK_FULL') {
                    console.error(chalk_1.default.yellow('Tip: Free up disk space and try again\n'));
                }
            }
            process.exit(1);
        }
        if (!quiet) {
            if (result.created) {
                console.log(chalk_1.default.green('\n✓ Character count management rule installed successfully!\n'));
                console.log(chalk_1.default.gray(`Location: ${result.path}\n`));
            }
            else if (result.updated) {
                console.log(chalk_1.default.green('\n✓ Character count management rule updated successfully!\n'));
                console.log(chalk_1.default.gray(`Location: ${result.path}\n`));
            }
            else if (result.skipped) {
                console.log(chalk_1.default.gray('\nℹ Character count rule already exists\n'));
                if (!force) {
                    console.log(chalk_1.default.gray('Use --force to replace the existing rule\n'));
                }
            }
        }
    }
    catch (error) {
        if (!quiet) {
            console.error(chalk_1.default.red('Error:'), error instanceof Error ? error.message : error);
        }
        process.exit(1);
    }
}
//# sourceMappingURL=install-rules.js.map