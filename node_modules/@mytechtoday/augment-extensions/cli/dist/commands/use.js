"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useCommand = useCommand;
const chalk_1 = __importDefault(require("chalk"));
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const module_loader_1 = require("../core/module-loader");
const module_system_1 = require("../utils/module-system");
async function useCommand(moduleName, options = {}) {
    try {
        const { version = 'latest', pin = false, json = false } = options;
        const modules = (0, module_system_1.discoverModules)();
        const module = modules.find((m) => m.fullName === moduleName || m.metadata.name === moduleName);
        if (!module) {
            if (json) {
                console.log(JSON.stringify({ error: `Module not found: ${moduleName}` }, null, 2));
            }
            else {
                console.error(chalk_1.default.red(`✗ Module not found: ${moduleName}`));
                console.log(chalk_1.default.gray('\nAvailable modules:'));
                modules.forEach((m) => console.log(chalk_1.default.gray(`  - ${m.fullName}`)));
            }
            process.exit(1);
        }
        const loader = new module_loader_1.ModuleLoader();
        const result = loader.load(module.path, { version });
        if (!result) {
            if (json) {
                console.log(JSON.stringify({ error: `Failed to load module ${moduleName} with version ${version}` }, null, 2));
            }
            else {
                console.error(chalk_1.default.red(`✗ Failed to load module ${moduleName} with version ${version}`));
                const availableVersions = loader.getAvailableVersions(module.path);
                if (availableVersions.length > 0) {
                    console.log(chalk_1.default.gray('\nAvailable versions:'));
                    availableVersions.forEach((v) => console.log(chalk_1.default.gray(`  - ${v}`)));
                }
            }
            process.exit(1);
        }
        if (pin) {
            await pinModuleVersion(moduleName, result.version);
        }
        if (json) {
            console.log(JSON.stringify({
                success: true,
                module: moduleName,
                version: result.version,
                strategy: result.resolution.strategy,
                pinned: pin,
                metadata: {
                    deprecated: result.metadata.deprecated,
                    breaking: result.metadata.breaking,
                    description: result.module.metadata.description
                }
            }, null, 2));
            return;
        }
        console.log(chalk_1.default.green(`✓ Successfully loaded ${moduleName} v${result.version}`));
        console.log(chalk_1.default.gray(`  Strategy: ${result.resolution.strategy}`));
        if (result.metadata.deprecated) {
            console.log(chalk_1.default.yellow('  ⚠ Warning: This version is deprecated'));
            if (result.metadata.deprecationMessage) {
                console.log(chalk_1.default.yellow(`    ${result.metadata.deprecationMessage}`));
            }
        }
        if (result.metadata.breaking) {
            console.log(chalk_1.default.yellow('  ⚠ Warning: This version contains breaking changes'));
        }
        if (pin) {
            console.log(chalk_1.default.cyan('  📌 Version pinned to config'));
        }
        console.log(chalk_1.default.gray(`\nModule: ${result.module.metadata.description}`));
        console.log(chalk_1.default.gray(`Type: ${result.module.metadata.type}`));
        console.log(chalk_1.default.gray(`Rules: ${result.module.rules.length}`));
        console.log(chalk_1.default.gray(`Examples: ${result.module.examples.length}`));
    }
    catch (error) {
        if (options.json) {
            console.log(JSON.stringify({ error: String(error) }, null, 2));
        }
        else {
            console.error(chalk_1.default.red('Error using module:'), error);
        }
        process.exit(1);
    }
}
async function pinModuleVersion(moduleName, version) {
    const configDir = path.join(process.cwd(), '.augment');
    const configPath = path.join(configDir, 'extensions.json');
    if (!fs.existsSync(configDir)) {
        fs.mkdirSync(configDir, { recursive: true });
    }
    let config = { modules: [] };
    if (fs.existsSync(configPath)) {
        try {
            config = JSON.parse(fs.readFileSync(configPath, 'utf-8'));
        }
        catch {
            config = { modules: [] };
        }
    }
    if (!Array.isArray(config.modules)) {
        config.modules = [];
    }
    const existingIndex = config.modules.findIndex((m) => m.name === moduleName);
    if (existingIndex >= 0) {
        config.modules[existingIndex].version = version;
        config.modules[existingIndex].pinnedAt = new Date().toISOString();
    }
    else {
        config.modules.push({
            name: moduleName,
            version,
            pinnedAt: new Date().toISOString()
        });
    }
    fs.writeFileSync(configPath, JSON.stringify(config, null, 2), 'utf-8');
}
//# sourceMappingURL=use.js.map