"use strict";
/**
 * Skill Commands
 *
 * Commands for managing and using skills.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.skillListCommand = skillListCommand;
exports.skillShowCommand = skillShowCommand;
exports.skillValidateCommand = skillValidateCommand;
exports.skillSearchCommand = skillSearchCommand;
exports.skillExecCommand = skillExecCommand;
exports.skillInjectCommand = skillInjectCommand;
exports.skillLoadBatchCommand = skillLoadBatchCommand;
exports.skillCacheClearCommand = skillCacheClearCommand;
exports.skillCacheStatsCommand = skillCacheStatsCommand;
exports.skillCreateMcpCommand = skillCreateMcpCommand;
const chalk_1 = __importDefault(require("chalk"));
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const skill_system_1 = require("../utils/skill-system");
/**
 * List all available skills
 */
async function skillListCommand(options = {}) {
    try {
        const skills = (0, skill_system_1.discoverSkills)();
        if (skills.length === 0) {
            console.log(chalk_1.default.yellow('No skills found'));
            return;
        }
        // Filter by category if specified
        const filteredSkills = options.category
            ? skills.filter(s => s.metadata.category === options.category)
            : skills;
        if (options.json) {
            console.log(JSON.stringify(filteredSkills.map(s => s.metadata), null, 2));
            return;
        }
        // Group by category
        const byCategory = filteredSkills.reduce((acc, skill) => {
            const cat = skill.metadata.category;
            if (!acc[cat])
                acc[cat] = [];
            acc[cat].push(skill);
            return acc;
        }, {});
        console.log(chalk_1.default.blue('\n📦 Available Skills\n'));
        for (const [category, categorySkills] of Object.entries(byCategory)) {
            console.log(chalk_1.default.cyan(`\n${category.toUpperCase()}`));
            console.log(chalk_1.default.gray('─'.repeat(50)));
            for (const skill of categorySkills) {
                const { id, name, version, tokenBudget, tags } = skill.metadata;
                console.log(`  ${chalk_1.default.green(id)} (v${version})`);
                console.log(`    ${name}`);
                console.log(`    Tokens: ${tokenBudget} | Tags: ${tags?.join(', ') || 'none'}`);
            }
        }
        console.log(chalk_1.default.gray('\n' + '─'.repeat(50)));
        console.log(chalk_1.default.blue(`Total: ${filteredSkills.length} skills\n`));
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error listing skills: ${error}`));
        process.exit(1);
    }
}
/**
 * Show details of a specific skill
 */
async function skillShowCommand(skillId, options = {}) {
    try {
        const skill = (0, skill_system_1.findSkill)(skillId);
        if (!skill) {
            console.error(chalk_1.default.red(`Skill not found: ${skillId}`));
            process.exit(1);
        }
        if (options.json) {
            console.log(JSON.stringify({ metadata: skill.metadata, content: skill.content }, null, 2));
            return;
        }
        const { metadata, content } = skill;
        console.log(chalk_1.default.blue(`\n📄 ${metadata.name} (${metadata.id})\n`));
        console.log(chalk_1.default.gray('─'.repeat(50)));
        console.log(`Version:      ${metadata.version}`);
        console.log(`Category:     ${metadata.category}`);
        console.log(`Token Budget: ${metadata.tokenBudget}`);
        console.log(`Priority:     ${metadata.priority || 'medium'}`);
        if (metadata.tags && metadata.tags.length > 0) {
            console.log(`Tags:         ${metadata.tags.join(', ')}`);
        }
        if (metadata.dependencies && metadata.dependencies.length > 0) {
            console.log(`Dependencies: ${metadata.dependencies.join(', ')}`);
        }
        if (metadata.cliCommand) {
            console.log(`CLI Command:  ${metadata.cliCommand}`);
        }
        if (metadata.mcpServer) {
            console.log(`MCP Server:   ${metadata.mcpServer}`);
        }
        console.log(chalk_1.default.gray('\n' + '─'.repeat(50)));
        console.log(chalk_1.default.cyan('\nContent:\n'));
        console.log(content);
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error showing skill: ${error}`));
        process.exit(1);
    }
}
/**
 * Validate a skill file
 */
async function skillValidateCommand(skillId) {
    try {
        const skill = (0, skill_system_1.findSkill)(skillId);
        if (!skill) {
            console.error(chalk_1.default.red(`Skill not found: ${skillId}`));
            process.exit(1);
        }
        const validation = (0, skill_system_1.validateSkillMetadata)(skill.metadata);
        if (validation.valid) {
            console.log(chalk_1.default.green(`✓ Skill ${skillId} is valid`));
        }
        else {
            console.log(chalk_1.default.red(`✗ Skill ${skillId} has validation errors:`));
            for (const error of validation.errors) {
                console.log(chalk_1.default.red(`  - ${error}`));
            }
            process.exit(1);
        }
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error validating skill: ${error}`));
        process.exit(1);
    }
}
/**
 * Search for skills by tags or keywords
 */
async function skillSearchCommand(query, options = {}) {
    try {
        const skills = (0, skill_system_1.discoverSkills)();
        const lowerQuery = query.toLowerCase();
        // Search in id, name, tags
        const results = skills.filter(skill => {
            const { id, name, tags } = skill.metadata;
            return (id.toLowerCase().includes(lowerQuery) ||
                name.toLowerCase().includes(lowerQuery) ||
                tags?.some(tag => tag.toLowerCase().includes(lowerQuery)));
        });
        if (results.length === 0) {
            console.log(chalk_1.default.yellow(`No skills found matching: ${query}`));
            return;
        }
        if (options.json) {
            console.log(JSON.stringify(results.map(s => s.metadata), null, 2));
            return;
        }
        console.log(chalk_1.default.blue(`\n🔍 Search Results for "${query}"\n`));
        console.log(chalk_1.default.gray('─'.repeat(50)));
        for (const skill of results) {
            const { id, name, category, tokenBudget } = skill.metadata;
            console.log(`  ${chalk_1.default.green(id)} (${category})`);
            console.log(`    ${name}`);
            console.log(`    Tokens: ${tokenBudget}`);
            console.log('');
        }
        console.log(chalk_1.default.gray('─'.repeat(50)));
        console.log(chalk_1.default.blue(`Found: ${results.length} skills\n`));
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error searching skills: ${error}`));
        process.exit(1);
    }
}
/**
 * Execute a skill's CLI command
 */
async function skillExecCommand(skillId, args = []) {
    try {
        const skill = (0, skill_system_1.findSkill)(skillId);
        if (!skill) {
            console.error(chalk_1.default.red(`Skill not found: ${skillId}`));
            process.exit(1);
        }
        if (!skill.metadata.cliCommand) {
            console.error(chalk_1.default.red(`Skill ${skillId} does not have a CLI command defined`));
            process.exit(1);
        }
        console.log(chalk_1.default.blue(`Executing skill: ${skill.metadata.name}`));
        console.log(chalk_1.default.gray(`Command: ${skill.metadata.cliCommand} ${args.join(' ')}\n`));
        // Execute the CLI command
        const { spawn } = await Promise.resolve().then(() => __importStar(require('child_process')));
        const [command, ...baseArgs] = skill.metadata.cliCommand.split(' ');
        const allArgs = [...baseArgs, ...args];
        const child = spawn(command, allArgs, {
            stdio: 'inherit',
            shell: true
        });
        child.on('error', (error) => {
            console.error(chalk_1.default.red(`Failed to execute command: ${error.message}`));
            process.exit(1);
        });
        child.on('exit', (code) => {
            if (code !== 0) {
                console.error(chalk_1.default.red(`Command exited with code ${code}`));
                process.exit(code || 1);
            }
        });
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error executing skill: ${error}`));
        process.exit(1);
    }
}
/**
 * Inject a skill's content into the AI context (with dynamic loading)
 */
async function skillInjectCommand(skillId, options = {}) {
    try {
        const loadedSkill = (0, skill_system_1.loadSkillDynamic)(skillId, {
            resolveDependencies: !options.noDeps,
            maxTokens: options.maxTokens || 50000,
            cache: true
        });
        if (!loadedSkill) {
            console.error(chalk_1.default.red(`Skill not found: ${skillId}`));
            process.exit(1);
        }
        const { skill, dependencies, totalTokens } = loadedSkill;
        if (options.json) {
            console.log(JSON.stringify({
                id: skill.metadata.id,
                name: skill.metadata.name,
                content: (0, skill_system_1.getSkillContentForInjection)(loadedSkill),
                tokenBudget: skill.metadata.tokenBudget,
                totalTokens,
                dependencies: dependencies.map(d => d.skill.metadata.id)
            }, null, 2));
            return;
        }
        // Output skill content for AI context injection
        console.log(chalk_1.default.blue(`\n# Skill: ${skill.metadata.name}\n`));
        console.log(chalk_1.default.gray(`ID: ${skill.metadata.id}`));
        console.log(chalk_1.default.gray(`Category: ${skill.metadata.category}`));
        console.log(chalk_1.default.gray(`Token Budget: ${skill.metadata.tokenBudget}`));
        console.log(chalk_1.default.gray(`Total Tokens (with deps): ${totalTokens}`));
        if (dependencies.length > 0) {
            console.log(chalk_1.default.gray(`Dependencies: ${dependencies.map(d => d.skill.metadata.id).join(', ')}`));
        }
        console.log(chalk_1.default.gray('\n' + '─'.repeat(50)));
        console.log((0, skill_system_1.getSkillContentForInjection)(loadedSkill));
        console.log(chalk_1.default.gray('─'.repeat(50)));
        console.log(chalk_1.default.green(`\n✓ Skill content ready for AI context injection (${totalTokens} tokens)`));
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error injecting skill: ${error}`));
        process.exit(1);
    }
}
/**
 * Load multiple skills in batch
 */
async function skillLoadBatchCommand(skillIds, options = {}) {
    try {
        const loadedSkills = (0, skill_system_1.loadSkillsBatch)(skillIds, {
            resolveDependencies: true,
            maxTokens: options.maxTokens || 50000,
            cache: true
        });
        if (loadedSkills.length === 0) {
            console.log(chalk_1.default.yellow('No skills loaded'));
            return;
        }
        const totalTokens = loadedSkills.reduce((sum, ls) => sum + ls.totalTokens, 0);
        if (options.json) {
            console.log(JSON.stringify({
                skills: loadedSkills.map(ls => ({
                    id: ls.skill.metadata.id,
                    name: ls.skill.metadata.name,
                    tokenBudget: ls.skill.metadata.tokenBudget,
                    totalTokens: ls.totalTokens,
                    dependencies: ls.dependencies.map(d => d.skill.metadata.id)
                })),
                totalTokens
            }, null, 2));
            return;
        }
        console.log(chalk_1.default.blue(`\n📦 Loaded ${loadedSkills.length} skills\n`));
        console.log(chalk_1.default.gray('─'.repeat(50)));
        for (const loadedSkill of loadedSkills) {
            const { skill, dependencies, totalTokens: skillTokens } = loadedSkill;
            console.log(chalk_1.default.green(`  ${skill.metadata.id}`));
            console.log(chalk_1.default.gray(`    ${skill.metadata.name}`));
            console.log(chalk_1.default.gray(`    Tokens: ${skillTokens} (base: ${skill.metadata.tokenBudget})`));
            if (dependencies.length > 0) {
                console.log(chalk_1.default.gray(`    Dependencies: ${dependencies.map(d => d.skill.metadata.id).join(', ')}`));
            }
        }
        console.log(chalk_1.default.gray('\n' + '─'.repeat(50)));
        console.log(chalk_1.default.blue(`Total tokens: ${totalTokens}\n`));
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error loading skills: ${error}`));
        process.exit(1);
    }
}
/**
 * Clear skill cache
 */
async function skillCacheClearCommand() {
    try {
        const stats = (0, skill_system_1.getSkillCacheStats)();
        (0, skill_system_1.clearSkillCache)();
        console.log(chalk_1.default.green(`✓ Cleared skill cache (${stats.size} skills)`));
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error clearing cache: ${error}`));
        process.exit(1);
    }
}
/**
 * Show skill cache statistics
 */
async function skillCacheStatsCommand() {
    try {
        const stats = (0, skill_system_1.getSkillCacheStats)();
        console.log(chalk_1.default.blue('\n📊 Skill Cache Statistics\n'));
        console.log(chalk_1.default.gray('─'.repeat(50)));
        console.log(`Cached skills: ${stats.size}`);
        if (stats.skills.length > 0) {
            console.log('\nCached skill IDs:');
            for (const skillId of stats.skills) {
                console.log(chalk_1.default.gray(`  - ${skillId}`));
            }
        }
        console.log(chalk_1.default.gray('\n' + '─'.repeat(50) + '\n'));
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error getting cache stats: ${error}`));
        process.exit(1);
    }
}
/**
 * Create a new MCP skill
 */
async function skillCreateMcpCommand(options) {
    try {
        const { name, description, category, tokenBudget = 2000, tags = [] } = options;
        // Validate category
        const validCategories = ['retrieval', 'transformation', 'analysis', 'generation', 'integration', 'utility'];
        if (!validCategories.includes(category)) {
            console.error(chalk_1.default.red(`Invalid category: ${category}`));
            console.log(chalk_1.default.yellow(`Valid categories: ${validCategories.join(', ')}`));
            process.exit(1);
        }
        // Generate skill ID from name
        const skillId = name.toLowerCase().replace(/[^a-z0-9-]/g, '-');
        // Determine skill file path
        const skillsDir = path.join(process.cwd(), 'skills', category);
        const skillPath = path.join(skillsDir, `${skillId}.md`);
        // Check if skill already exists
        if (fs.existsSync(skillPath)) {
            console.error(chalk_1.default.red(`Skill already exists: ${skillPath}`));
            process.exit(1);
        }
        // Ensure skills directory exists
        if (!fs.existsSync(skillsDir)) {
            fs.mkdirSync(skillsDir, { recursive: true });
        }
        // Generate skill content
        const mcpTags = ['mcp', ...tags];
        const cliCommand = `augx-${skillId}`;
        const skillContent = `---
id: ${skillId}
name: ${name}
version: 1.0.0
category: ${category}
tokenBudget: ${tokenBudget}
priority: medium
tags: [${mcpTags.join(', ')}]
dependencies: []
cliCommand: ${cliCommand}
${options.package ? `mcpServer: ${options.package}` : ''}
---

# ${name}

## Purpose

${description}

## Installation

\`\`\`bash
# Install MCP server
${options.package ? `npm install -g ${options.package}` : '# Add installation command here'}
\`\`\`

## Configuration

Add configuration steps here (authentication, API keys, etc.)

## Available Tools

List the tools provided by this MCP server:

1. **Tool 1** - Description
2. **Tool 2** - Description
3. **Tool 3** - Description

## CLI Command

\`\`\`bash
${cliCommand} [options]
\`\`\`

### Options

- \`--option1\` - Description
- \`--option2\` - Description

## Examples

### Example 1: Basic Usage

\`\`\`bash
${cliCommand} --option1 value
\`\`\`

### Example 2: Advanced Usage

\`\`\`bash
${cliCommand} --option1 value --option2 value
\`\`\`

## Performance

- **Query Time**: ~X seconds
- **Token Usage**: ~X tokens
- **Rate Limits**: X requests/minute

## Best Practices

1. Best practice 1
2. Best practice 2
3. Best practice 3

## Troubleshooting

### Issue 1

**Problem**: Description of problem

**Solution**: Description of solution

### Issue 2

**Problem**: Description of problem

**Solution**: Description of solution
`;
        // Write skill file
        fs.writeFileSync(skillPath, skillContent, 'utf-8');
        console.log(chalk_1.default.green(`✅ Skill created successfully!`));
        console.log(chalk_1.default.blue(`\n📄 Skill file: ${skillPath}`));
        console.log(chalk_1.default.gray(`\nNext steps:`));
        console.log(chalk_1.default.gray(`1. Edit ${skillPath} to add MCP-specific details`));
        console.log(chalk_1.default.gray(`2. Validate skill: augx skill validate ${skillId}`));
        console.log(chalk_1.default.gray(`3. Test skill: augx skill show ${skillId}`));
        console.log(chalk_1.default.gray(`4. Update documentation: skills/README.md`));
    }
    catch (error) {
        console.error(chalk_1.default.red(`Error creating MCP skill: ${error}`));
        process.exit(1);
    }
}
//# sourceMappingURL=skill.js.map